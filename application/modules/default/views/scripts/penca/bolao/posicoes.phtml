<?php
    $teams = $this->teams;
    $champ = $this->champ;

    $champion = $this->championship;

    $t = Zend_Registry::get('translate');
//    print_r($champion['ch_sec1_fim']);
    
    if (isset($teams)) {
        
        $p = new Helpers_Posicoes();
        $p->champion = $champion;
        $p->champ = $champ;
        $p->teams = $teams;
        $p->base = $this->baseUrl();
        $p->posicoes();
    
    }
